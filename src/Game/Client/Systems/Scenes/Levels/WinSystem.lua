local WinSystem = require(game.ReplicatedStorage.Source.System).extend()

-- Aqui se debe capturar cada vez que se choca con un target y comparar cuantos targets quedan,
-- si quedan 0, entonces se ha ganado la ronda

return WinSystem